let area =
    var side = 9 in
    side * side # 81

let sum =
    var a = 1 in
    var b = 2 in
    a + b # 3

let polynomial = a -> b -> c -> x ->
    var ax2 = a * x * x in
    var bx = b * x in
    ax2 + bx + c
